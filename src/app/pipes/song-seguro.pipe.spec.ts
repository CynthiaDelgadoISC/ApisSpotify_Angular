import { SongSeguroPipe } from './song-seguro.pipe';

describe('SongSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new SongSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
